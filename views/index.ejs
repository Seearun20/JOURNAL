<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Personal Notes App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-900 antialiased">
    <div class="container mx-auto px-4 py-8">
        <header class="mb-12 text-center">
            <h1 class="text-5xl font-bold text-white mb-4 tracking-tight">
                Your Personal Notes App
                <span class="block text-sm text-gray-400 mt-2 font-normal">Likhdo apne dil ki..</span>
            </h1>
        </header>

        <main class="max-w-4xl mx-auto">
            <section class="bg-zinc-800 rounded-xl shadow-2xl p-8 mb-10">
                <form action="/create" method="POST" class="space-y-6">
                    <div>
                        <input 
                            type="text" 
                            name="title" 
                            placeholder="Task Title" 
                            required
                            class="w-full px-5 py-3 rounded-lg bg-zinc-700 text-white placeholder-gray-400 focus:ring-2 focus:ring-blue-500 transition duration-300"
                        >
                    </div>
                    <div>
                        <textarea 
                            name="details" 
                            placeholder="Write your ❤️ Out..." 
                            rows="4"
                            required
                            class="w-full px-5 py-3 rounded-lg bg-zinc-700 text-white placeholder-gray-400 resize-none focus:ring-2 focus:ring-blue-500 transition duration-300"
                        ></textarea>
                    </div>
                    <div class="text-right">
                        <button 
                            type="submit" 
                            class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg transition duration-300 ease-in-out transform hover:scale-105 focus:outline-none focus:ring-2 focus:ring-blue-500"
                        >
                            Save your Memory💕
                        </button>
                    </div>
                </form>
            </section>

            <section class="tasks-container">
                <% if(files.length > 0) { %>
                    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
                        <% files.forEach(function(val){ %>
                            <div class="bg-zinc-800 rounded-xl p-6 shadow-lg hover:shadow-2xl transition duration-300 transform hover:-translate-y-2">
                                <h2 class="text-2xl font-semibold text-white mb-4 truncate">
                                    <%= val %>
                                </h2>
                                <a 
                                    href="/files/<%= val %>" 
                                    class="inline-block mt-4 text-blue-400 hover:text-blue-300 transition duration-300 font-medium"
                                >
                                    View Details →
                                </a>
                            </div>
                        <% }); %>
                    </div>
                <% } else { %>
                    <div class="text-center py-12">
                        <p class="text-gray-600 text-xl">
                            No stories yet. Save your First Story📔!
                        </p>
                    </div>
                <% } %>
            </section>
        </main>
    </div>
</body>
</html>